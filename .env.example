# Copy this file to .env and adjust as needed. .env itself is gitignored.

SECRET_KEY=dev-key-change-for-any-real-deployment
FLASK_DEBUG=True
DEFAULT_PASS_THRESHOLD=50.0
DEFAULT_TARGET_CLASS_AVERAGE=60.0
