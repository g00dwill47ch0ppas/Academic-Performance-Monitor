# Matches the reference repo's stack (Flask + PuLP + pandas + numpy + openpyxl + dotenv)
Flask==3.1.3
python-dotenv==1.2.2
pandas==2.2.2
numpy==1.26.4
PuLP==2.9.0
openpyxl==3.1.5

# Added beyond the reference stack, and why:
# PuLP is LP/MILP-only and cannot express Algorithm 3's non-linear objective —
# scipy is required to keep that model mathematically unmodified from the source paper.
scipy==1.13.1

# Testing
pytest==8.2.2
